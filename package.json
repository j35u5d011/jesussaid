{
  "name": "jesussaid",
  "version": "1.0.0",
  "description": "The words of Jesus, every day — static PWA",
  "scripts": {
    "build": "node scripts/generate.js",
    "build:local": "USE_LOCAL_CSV=1 node scripts/generate.js"
  },
  "engines": {
    "node": ">=18"
  }
}
